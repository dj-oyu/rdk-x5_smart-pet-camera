[project]
name = "detector"
version = "0.1.0"
description = "YOLO detector daemon for Smart Pet Camera"
requires-python = ">=3.10,<3.14"
dependencies = [
    "numpy>=1.26.0",
    "opencv-python>=4.8.0",
    "scipy>=1.11.0",
    "hobot-dnn-rdkx5>=3.0.0",
]
