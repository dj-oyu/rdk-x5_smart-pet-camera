---
paths: src/detector/**/*.py
---

## 検出結果の書き込みルール

検出デーモンは **検出が1件以上ある場合のみ** 共有メモリに書き込む。

- 検出ゼロの場合は `write_detection_result()` を呼ばない
- これは意図的な設計（CPU負荷軽減、不要な書き込み削減）
- Consumer側は version が更新されない = 新しい検出がない、と解釈する
